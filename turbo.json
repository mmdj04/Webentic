{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "content:build", "build:registry", "build:llms"],
      "outputs": [".next/**", "dist/**", "public/r/**", "public/llms.txt", ".contentlayer/**"],
      "inputs": ["$TURBO_DEFAULT$", "!*.test.*"]
    },
    "build:app": {
      "dependsOn": ["^build", "content:build"],
      "outputs": [".next/**"],
      "inputs": ["$TURBO_DEFAULT$", "!*.test.*"]
    },
    "content:build": {
      "outputs": [".contentlayer/**"]
    },
    "build:registry": {
      "outputs": ["public/r/**"]
    },
    "build:llms": {
      "outputs": ["public/llms.txt"]
    },
    "dev": {
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    }
  },
  "remoteCache": {
    "enabled": true
  }
}
